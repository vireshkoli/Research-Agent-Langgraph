# Environments & secrets
.env
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Runtime artifacts
data/
workspace/
*.sqlite3
*.db
traces/local/

# Dev-only search cache (never used for the official eval — see evals/REPORT.md)
.cache/

# evals/results/ IS committed — it is the audit trail behind every README number.

# OS / editor
.DS_Store
.idea/
.vscode/

# Build
dist/
*.egg-info/

# Ledger and dev cache (never committed)
.spend.json
.spend.json.tmp

# Eval checkpoint (an in-flight run's partial state, not a result)
evals/results/.*.partial.jsonl
